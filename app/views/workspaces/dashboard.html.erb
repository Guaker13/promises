<div class="container">
 <% @workspaces.each do |workspace| %>
  <div class="col-xs-12 col-lg-4">
    <h1><%= workspace.name %></h1>
    <p><%= workspace.capacity %> - <%= workspace.price %>
    /<%= workspace.price_unit %></p>
  </div>
 <% end -%>
</div>
