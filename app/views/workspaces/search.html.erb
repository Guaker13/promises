
<div class="container padded">
	<h2>Searching for:<h2>

		<%= render 'shared/search_form' %>
		<%= link_to "Workspaces", "/workspaces", class: "btn btn-primary" %>
		<%= link_to "The Hottest Workspaces", "/workspaces/hot", class: "btn btn-danger" %>

	</div>

	<div class="card-container">
		<div class="container">
			<div class="row">
				<% @show.each do |work|  %>

				<%= render 'card', workspace: work %>
				
				<% end  %>

			</div>
		</div>
	</div>
